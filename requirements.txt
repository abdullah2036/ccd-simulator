streamlit==1.29.0
pandas==2.1.4
numpy==1.26.2
scikit-learn==1.3.2

# Optional: For enhanced ML predictions (auto-trains on first run)
# These are optional - app works without them

# Optional: For local LLM offline mode (privacy feature)
 transformers>=4.35.0
 torch>=2.1.0
# Uncomment above lines to enable offline LLM document processing
